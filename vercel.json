{
  "buildCommand": "pnpm --filter @workspace/solomax run build:vercel",
  "outputDirectory": "artifacts/solomax/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
